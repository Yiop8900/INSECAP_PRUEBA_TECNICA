@model INSECAP.Models.AsignacionCursosPro

@{
    ViewData["Title"] = "Create";
}

<h1>Asignacion del Profesor</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

        <script>
            // Función para mostrar una alerta de error
            @if (TempData.ContainsKey("MensajeError"))
            {
                var mensajeError = TempData["MensajeError"] as string;
                <text>
                    Swal.fire({
                        icon: 'error',
                        title: 'Oops...',
                        text: '@mensajeError'
                    });
                </text>
            }

                // Función para mostrar una alerta de éxito
            @if (TempData.ContainsKey("MensajeExito"))
            {
                var mensajeExito = TempData["MensajeExito"] as string;
                <text>
                    Swal.fire({
                        icon: 'success',
                        title: 'Éxito!',
                        text: '@mensajeExito'
                    }).then(function () {
                        // Redirige al usuario a la página "index"
                        window.location.href = '@Url.Action("Index", "AsignacionCursosProes")';
                    });
                </text>
            }
        </script>
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group" style="margin-top:6px;">
                <label asp-for="CodigoCurso" class="control-label">CODIGO DEL CURSO</label>
                <select asp-for="CodigoCurso" class ="form-control form_alumno" asp-items="ViewBag.CodigoCurso"></select>
            </div>
            <div class="form-group" style="margin-top:6px;">
                <label asp-for="RunProfesor" class="control-label"> RUT DEL PROFESOR</label>
                <select asp-for="RunProfesor" class ="form-control form_alumno" asp-items="ViewBag.RunProfesor"></select>
            </div>
            <div class="form-group" style="margin-top:6px;">
                <label asp-for="IdBimestre" class="control-label">BIMESTRE</label>
                <select asp-for="IdBimestre" class ="form-control form_alumno" asp-items="ViewBag.IdBimestre"></select>
            </div>
            <div class="form-group" style="margin-top:10px;">
                <input type="submit" value="Guardar" class="btn btn-primary" /> | <a asp-action="Index" class="btn btn-dark">Volver al Listado</a>
            </div>
        </form>
    </div>
    <div class="col-md-8">
        <img src="~/img/Tabla_Bimestres.png" alt="Alternate Text" class="imagen_guardar_alumno"/>
    </div>
</div>



@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
